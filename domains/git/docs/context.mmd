flowchart LR
    Git[git domain] --> Workspace[workspace]
    Git --> Artifact[artifact]
    Git --> Harness[harness]

    Daemon[core-daemon] --> Snapshot[GitReviewSnapshot]
    Snapshot --> Status[branch/head/status]
    Snapshot --> Files[changed files]
    Snapshot --> Diff[bounded diff]
    Snapshot --> Preview[bounded file preview]

    Snapshot -. read-only .-> Worktree[(Git worktree)]
