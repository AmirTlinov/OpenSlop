# shared-kernel

`shared-kernel/` держим крошечным. Сюда попадают только действительно общие примитивы двух и более доменов: ids, timestamps, blob refs, small value objects.

Если новая вещь тащит политику, lifecycle или provider semantics, ей не сюда.
