# ACCEPTANCE

- Center timeline fallback больше не создаёт synthetic proof cards.
- Generic shell header больше не показывает hardcoded `S04` badge.
- No-session, empty-transcript и unavailable-transcript paths имеют first-class native empty surface.
- Live transcript path остаётся item-based и не получает empty state.
- Empty state не содержит `S04` или proof-target storytelling.
- `make smoke-timeline-empty-state` зелёный.
